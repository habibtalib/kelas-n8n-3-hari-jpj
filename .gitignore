# Local env & secrets — never commit
templates/.env
.env
*.local

# Python (slide generators)
__pycache__/
*.pyc
venv/
.venv/

# Slides — not tracked in this repo
slides/

# Speaker notes (trainer-only) — not tracked
nota-penceramah.md

# Template workflow JSONs — not tracked (import into n8n instead)
templates/workflows/*.json

# OS
.DS_Store
Thumbs.db
CLAUDE.md